fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
pyjwt[crypto]==2.10.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==43.0.3
celery==5.4.0
redis==5.2.0
httpx==0.28.0
python-multipart==0.0.18
boto3==1.35.92
sentry-sdk[fastapi,celery]==2.18.0
pytest==8.3.4
pytest-asyncio==0.24.0
# Dev-only: ships with the `watchmedo` CLI used by docker-compose.yml's
# worker service to auto-restart Celery on .py file changes. The prod
# compose runs celery directly; the package is harmless extra bytes
# there (~100 KB) but avoids needing a separate dev requirements file.
watchdog==6.0.0
